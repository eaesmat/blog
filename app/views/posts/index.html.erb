  <ul class="pt-3">
      <div class="container border rounded-3 p-3 col-8 justify-content-center">
        <div class="d-inline ">
          <img src= '<%= @user.photo %>' width="100" height="100" class="" />
        </div>
        <h3 class="d-inline ps-5"><%= @user.name %></h3>
        <p class="text-end">Number of Posts: <%=@user.post_counter%></p>
      </div>
    </ul>


  <% @posts = Post.where(user_id: @user.id) %>
</p>
<ul>
    <% @posts.each do |post| %>
        <div class="container border rounded-3 p-3 col-8 justify-content-center mt-4">
           <h4> <%= post.title %> </h4>
        <p> <%= post.text %> </p>
        <p class="text-end"> <small>Likes: <%= post.likes_counter%></small> , <small>Comments : <%= post.comments_counter%></small></p>
        </div>
         <div class="container border rounded-3 p-3 col-8 justify-content-center mt-4">
             <h6>Comments:</h6>
             <% post.most_recent_comments.each do |comment| %>

                <p class="d-inline fw-blod"> <%= comment.user.name%>: </p>
                <small><%= comment.text%> </small> <br>
            <%end%>
         </div>
    <% end %>
</ul>
